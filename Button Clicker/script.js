/* Hide elements */ 
function hide(element) {
    element.remove();
}

/* Logout */
function logout(element) {
    element.innerText = "Logout";
}

function popupbox() {
    alert('Ninja was liked')
}